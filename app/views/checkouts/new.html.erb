<!-- This view renders a form for creating a new checkout. -->
<!--
- Displays the header "New Checkout" at the top.
- Uses `form_with` to bind the form to the `@checkout` object.
- If there are validation errors, they are displayed in an alert box with a list of error messages.
- Includes a dropdown to select a book, showing only available books (`Book.available`).
- Provides input fields for `start_date` and `due_date` with date pickers, both required.
- Contains a submit button to create the checkout and a link to cancel, redirecting to the checkouts index page.
-->
