<!-- This view renders a form for editing a book checkout. -->
<!--
- Displays a header "Edit Book Checkout" at the top.
- Uses `simple_form_for` to bind the form to the `@checkout` object.
- If there are validation errors, they are displayed in a styled error message section.
- Provides dropdowns to select a user (by email) and a book (by title) for the checkout.
- Includes input fields for `start_date`, `due_date`, and `return_date` (renew date).
- Includes a checkbox for marking the book as returned (`is_returned`).
- Contains a submit button to update the checkout and a link to navigate back to the checkouts index page.
-->
