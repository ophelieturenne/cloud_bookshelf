<%# This view displays a list of libraries the user is associated with. %>

<%# Header Section %>
<!--
- Displays the header "Your Libraries" at the top of the page.
-->

<%# Libraries Table Section %>
<!--
- Renders a table with two columns:
  - "Library Name": Displays the name of each library.
  - "Actions": Provides buttons for actions related to each library.
- Iterates through the `@libraries` collection:
  - Displays the name of each library.
  - Includes a "View Books" button for each library, linking to the list of books in that library.
-->
